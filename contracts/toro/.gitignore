# Aiken build artifacts
build/
artifacts/
docs/

# Node.js dependencies (keep package.json & lock for the mock script)
node_modules/

# Generated keys — NEVER commit these
*.skey
*.addr
*.vkey

# Deployment output / temp files
deploy-out/
deploy-manifest.json

# env
.env